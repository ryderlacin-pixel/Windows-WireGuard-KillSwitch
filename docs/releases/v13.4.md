# v13.4 — Privacy Hardening

Browser + Windows privacy on top of v13.3 WebRTC guard. Kill switch unchanged.

## Browser (Chrome / Edge / Brave / Firefox)

- WebRTC restricted (v13.3)
- Third-party cookies blocked, DNT, sync/autofill off, metrics off
- Firefox: `privacy.resistFingerprinting`, strict content blocking, tracking protection

## Windows

- Telemetry off, advertising ID off, activity feed off
- Location/Cortana/cloud consumer features restricted
- App advertising ID force-denied
- `DiagTrack` + `dmwappushservice` disabled

## Scripts

- `privacy-hardening-guard.ps1` — repair + vault re-apply
- `webrtc-leak-guard.ps1` — forwarder
- `repair-wmi-subscription.ps1` — idempotent WMI filter/consumer/binding repair

## WMI (v13.4.1 fix)

- Install uses direct `Install-WmiSubscription` (no `Start-Job` timeout false-negative)
- `Test-WmiSubscriptionActive` verifies filter + consumer + binding
- Anti-tamper `Restore-WmiSubscription` uses filtered CIM (no full-namespace scan)

## Privacy score (project scope)

| Metric | v13.3 | v13.4 |
|--------|-------|-------|
| Leak prevention | 8.5/10 | 8.5/10 |
| General anonymity | ~7/10 | **~9/10** |

Restart browsers after install.

```powershell
.\scripts\safe-live-verify.ps1
```