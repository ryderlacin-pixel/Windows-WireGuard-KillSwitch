# v12.0 — Ultimate Release

## Highlights
- Unified `$WG_KS_VERSION = 12.0` across all generated scripts
- WMI: single OR query watches `powershell.exe` + `pwsh.exe`
- PID singleton: monitor/repair validate command-line, not PID reuse
- Tunnel recovery: mutex + `sc.exe start` + 30s poll, repair task 15min limit
- Emergency unbrick: monitor never bricks internet indefinitely
- GPO/post-reboot: parameterized tunnel service name
- `safe-live-verify.ps1`: production gate (read-only, no tunnel stop)
- Destructive tests require `-ConfirmDisruptsInternet` / `-ConfirmDisruptiveTests`

## Test gates
- Offline CI: 90 assertions × 3
- Live safe: `post-install-verify.ps1`, `safe-live-verify.ps1`, `security-audit.ps1`