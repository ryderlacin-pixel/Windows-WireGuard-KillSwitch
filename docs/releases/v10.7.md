## v10.7 — 9.5 quality gate (layer sync + parse fix)

### Critical fix

**v10.6 could not compile** on PowerShell 5.1: `function Get-MainMonitorProcs() { return Get-MonitorShellProcs() }` triggered a parser error (`An expression was expected after '('`). Removed the alias; installer uses `Get-MonitorShellProcs` directly.

### Improvements

| Area | Change |
|------|--------|
| **repair.ps1** | `Sync-KillSwitchState` — applies block/open using same `Test-SafeToOpen` as monitor |
| **GPO boot** | Waits for `Test-SafeToOpen` (90s), logs zombie vs down, uses `Start-HiddenScript` |
| **Ensure-ServerRule** | `Set-ServerRule` only when IPs change or rule missing (no 5s netsh spam) |
| **Test-Internet** | 2-of-3 hosts (1.1.1.1, 1.0.0.1, 8.8.8.8) |
| **Shell** | `Get-PreferredShell` + `Start-HiddenScript` in repair/WMI/service/GPO |
| **Installer** | `$ErrorActionPreference = Continue`; custom param guard |
| **STEP 19** | Health check with `Test-SafeToOpen` + block rule verification |
| **Tests** | `scripts/test-suite.ps1` — 30 assertions (compile, extract, mutex, patterns) |

### Upgrade

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
.\install.ps1
```