# v15.2.9 - Install final-check warnings fix

Fixes the 3 warnings seen at end of install:

| Warning | Fix |
|---------|-----|
| WG-RepairTask: 1 trigger (expected 2) | `Register-RepairTaskDualTrigger` - boot 30s + every 2min via task XML |
| Registry backup: incomplete | `Refresh-RegistryTaskBackups` at STEP 19 + STEP 20 |
| Script integrity vault: mismatch | Vault finalized **before** services start; re-hashed after privacy guards; `Get-ScriptSha256` unlocks hidden files |
| Monitor: not yet running | 24s wait loop before final check |
| Can't open service (NSSM cleanup) | Only run NSSM remove when `sc query` shows STATE |

## Install tip

Use `-NoPause` to skip "Press Enter":

```powershell
.\install.ps1 -NoPause
```